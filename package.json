{
  "name": "routtefe",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p dist && cp index.html dist/index.html && cp firebase-applet-config.json dist/ 2>/dev/null || true",
    "lint": "node --check server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "firebase": "^12.19.0"
  }
}
